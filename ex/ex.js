/*var controller;
  $(document).ready(function($) {
    // init controller
    controller = new ScrollMagic();

    var scene = new ScrollScene({triggerElement: "#trigger1", duration: 5000})
            .setPin("#left")
            .addTo(controller);
  });
*/
